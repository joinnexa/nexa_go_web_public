# Weekly SEO monitoring — Nexa Go

1. **GSC Performance** — queries containing `nexa go`, `nexago`.
2. **Indexing** — home + `/about-nexa-go/` variants Indexed.
3. **Sitemap** — `nexago.ma/sitemap.xml` success.
4. **Deploy check** — `public/about-nexa-go/**` still present in production build (`dist/` after `vite build`).

If SPA rewrite ever shadows static folders, confirm Vercel serves `about-nexa-go/index.html` (URL Inspection Live Test).
